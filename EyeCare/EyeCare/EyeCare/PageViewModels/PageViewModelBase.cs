using EyeCare.ViewModels;

namespace EyeCare.PageViewModels
{
    public class PageViewModelBase : ViewModelBase
    {
        //public Settings Settings => Settings.Current;

        //private static UserViewModel _loggedUser;
        //public UserViewModel LoggedUser => 
        //    _loggedUser ?? (_loggedUser = new UserViewModel(Settings.LoggedUser)) ;
    }
}